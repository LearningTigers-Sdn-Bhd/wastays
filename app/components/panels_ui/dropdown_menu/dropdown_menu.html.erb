<div id="<%= @id %>"
     class="dropdown-menu-root"
     data-controller="ui--dropdown-menu"
     data-ui--dropdown-menu-placement-value="<%= floating_placement %>"
     data-ui--dropdown-menu-offset-value="<%= @offset %>"
     data-action="pointerdown@window->ui--dropdown-menu#onWindowPointerDown">
  <%= trigger %>

  <div id="<%= menu_id %>"
       role="menu"
       tabindex="-1"
       popover="manual"
       class="<%= tw_merge("dropdown-menu", @class) %>"
       aria-labelledby="<%= trigger_id %>"
       data-ui--dropdown-menu-target="menu"
       data-action="keydown->ui--dropdown-menu#onMenuKeydown click->ui--dropdown-menu#onMenuClick toggle->ui--dropdown-menu#onPopoverToggle">
    <% if header? %>
      <div class="dropdown-menu__header" role="presentation"><%= header %></div>
    <% end %>
    <%= safe_join(entries) %>
  </div>
</div>

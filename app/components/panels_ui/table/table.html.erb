<div class="<%= wrapper_classes %>">
  <%= tag.table(**table_attributes) do %>
    <caption class="sr-only"><%= @caption %></caption>
    <thead><%= header %></thead>
    <tbody><%= body %></tbody>
    <% if footer? %>
      <tfoot><%= footer %></tfoot>
    <% end %>
  <% end %>
</div>

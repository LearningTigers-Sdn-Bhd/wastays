<%= tag.div(**root_attributes) do %>
  <%= bar if bar? %>
  <%= log %>
  <div class="public-chat__footer">
    <%= alert if alert? %>
    <%= tag.div(id: PublicUI::Chat::Panel::INPUT_REGION_ID) do %>
      <% if secure_input? %>
        <%= secure_input %>
      <% else %>
        <%= quick_replies if quick_replies? %>
        <%= composer %>
      <% end %>
    <% end %>
  </div>
<% end %>

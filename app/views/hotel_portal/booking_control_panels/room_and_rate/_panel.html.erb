<% if presenter.group_overview? %>
  <% rows = presenter.group_room_rate_rows %>
  <% issues = presenter.group_room_rate_issues %>
  <section aria-labelledby="room-rate-heading">
    <header class="border-b border-border px-5 py-4">
      <p class="text-xs font-semibold text-muted-foreground">Booking No. <%= presenter.group_booking_number %></p>
      <h2 id="room-rate-heading" class="mt-1 text-lg font-bold text-foreground">Group Room &amp; Rate</h2>
      <p class="mt-1 text-sm text-muted-foreground">The room and agreed rate for each night across the group.</p>
    </header>

    <div class="overflow-x-auto">
      <table class="w-full min-w-[860px] text-left text-sm">
        <thead class="border-b border-border bg-muted text-xs font-semibold text-muted-foreground">
          <tr><th class="px-5 py-3">Booking No.</th><th class="px-5 py-3">Stay Date</th><th class="px-5 py-3">Room Type</th><th class="px-5 py-3">Room</th><th class="px-5 py-3">Rate Plan</th><th class="px-5 py-3 text-right">Nightly Rate</th></tr>
        </thead>
        <tbody class="divide-y divide-border">
          <% rows.each do |row| %>
            <tr>
              <td class="px-5 py-3 font-semibold text-foreground"><%= row.booking_reference %></td>
              <td class="whitespace-nowrap px-5 py-3 font-semibold text-foreground"><%= row.date.strftime("%d %b %Y") %></td>
              <td class="px-5 py-3 text-foreground"><%= row.room_type %></td>
              <td class="px-5 py-3 text-foreground"><%= row.room %></td>
              <td class="px-5 py-3 text-foreground"><%= row.rate_plan %></td>
              <td class="px-5 py-3 text-right font-semibold tabular-nums <%= row.rate_missing ? 'text-amber-700' : 'text-foreground' %>"><%= row.nightly_rate %></td>
            </tr>
          <% end %>

          <% issues.each do |issue| %>
            <tr><td class="px-5 py-4 font-semibold text-foreground"><%= issue[:booking_reference] %></td><td colspan="5" class="px-5 py-4 text-muted-foreground"><%= issue[:message] %></td></tr>
          <% end %>

          <% if rows.empty? && issues.empty? %>
            <tr><td colspan="6" class="px-5 py-10 text-center text-muted-foreground">No room-rate rows are available for this group.</td></tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </section>
<% else %>
  <% rows = presenter.room_rate_rows %>
  <% empty_message = presenter.room_rate_empty_message %>
  <section aria-labelledby="room-rate-heading">
    <header class="flex flex-col gap-3 border-b border-border px-5 py-4 sm:flex-row sm:items-start sm:justify-between">
      <div><p class="text-xs font-semibold text-muted-foreground">Booking No. <%= presenter.booking_number %></p><h2 id="room-rate-heading" class="mt-1 text-lg font-bold text-foreground">Room &amp; Rate</h2><p class="mt-1 text-sm text-muted-foreground">The room and agreed rate for each night of this booking.</p></div>
      <div class="flex flex-wrap gap-2">
        <%= link_to "Change Room", hotel_booking_transaction_edit_booking_timeline_path(current_hotel, booking), data: { turbo_frame: "offcanvas_drawer", offcanvas_variant: "right" }, class: "rounded-md border border-border-interactive bg-card px-3 py-2 text-sm font-semibold text-foreground hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600" %>
        <%= link_to "Change Rate", hotel_booking_control_panel_path(current_hotel, booking, tab: "room_and_rate", alert_action: "change_rate"), data: { turbo_frame: "booking_control_panel_workspace", turbo_action: "advance", warning_dialog_trigger: "change-rate" }, class: "rounded-md bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground hover:bg-primary-hover focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600" %>
      </div>
    </header>

    <div class="overflow-x-auto">
      <table class="w-full min-w-[720px] text-left text-sm">
        <thead class="border-b border-border bg-muted text-xs font-semibold text-muted-foreground">
          <tr><th class="px-5 py-3">Stay Date</th><th class="px-5 py-3">Room Type</th><th class="px-5 py-3">Room</th><th class="px-5 py-3">Rate Plan</th><th class="px-5 py-3 text-right">Nightly Rate</th></tr>
        </thead>
        <tbody class="divide-y divide-border">
          <% if rows.any? %>
            <% rows.each do |row| %>
              <tr>
                <td class="whitespace-nowrap px-5 py-3 font-semibold text-foreground"><%= row.date.strftime("%d %b %Y") %></td>
                <td class="px-5 py-3 text-foreground"><%= row.room_type %></td>
                <td class="px-5 py-3 text-foreground"><%= row.room %></td>
                <td class="px-5 py-3 text-foreground"><%= row.rate_plan %></td>
                <td class="px-5 py-3 text-right font-semibold tabular-nums <%= row.rate_missing ? 'text-amber-700' : 'text-foreground' %>"><%= row.nightly_rate %></td>
              </tr>
            <% end %>
          <% else %>
            <tr><td colspan="5" class="px-5 py-10 text-center text-muted-foreground"><%= empty_message || "No room-rate rows are available for this booking." %></td></tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </section>
<% end %>

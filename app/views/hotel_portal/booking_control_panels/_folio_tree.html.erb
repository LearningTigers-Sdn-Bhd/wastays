<div class="p-3">
  <p class="mb-2 text-[11px] font-black uppercase tracking-wider text-muted-foreground">Folios</p>
  <% if presenter.folio_tree_rows.any? %>
    <div class="space-y-1">
      <% presenter.folio_tree_rows.each do |row| %>
        <%= link_to hotel_booking_control_panel_path(current_hotel, booking, tab: presenter.active_tab, folio_id: row.id, room_id: params[:room_id]), class: "block rounded-lg px-3 py-2 text-sm #{row.active ? "bg-blue-600 text-primary-foreground" : "text-foreground hover:bg-muted"}" do %>
          <span class="block font-bold"><%= row.label %></span>
          <span class="block truncate text-xs opacity-75"><%= row.description %></span>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <p class="rounded-lg border border-dashed border-border px-3 py-4 text-sm text-muted-foreground">No folios available.</p>
  <% end %>
</div>

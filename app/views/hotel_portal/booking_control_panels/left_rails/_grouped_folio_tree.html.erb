<% if presenter.can_manage_folio_windows? %>
  <div class="border-b border-border px-4 py-3">
    <%= link_to new_folio_window_hotel_booking_control_panel_path(current_hotel, booking),
        data: { turbo_frame: "offcanvas_drawer", offcanvas_variant: "right" },
        class: "inline-flex w-full items-center justify-center rounded-md bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground hover:bg-primary-hover focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600" do %>
      + Add Folio
    <% end %>
  </div>
<% end %>

<%= render "hotel_portal/booking_control_panels/left_rails/booking_entity_tree",
    booking: booking,
    presenter: presenter,
    groups: presenter.grouped_folio_tree_groups,
    aria_label: "Bookings and folios",
    empty_label: "No folios available." %>

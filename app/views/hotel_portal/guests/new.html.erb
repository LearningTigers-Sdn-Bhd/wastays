<%= render "form_sheet",
      guest: @guest,
      title: "New guest record",
      description: "Create a permanent record so this guest's stays are tracked across bookings.",
      submit_label: "Create guest record",
      return_to: params[:return_to] %>

<div class="space-y-4">
  <%= render "record_header", guest: @guest, presenter: @presenter %>
  <%= render "booking_history", guest: @guest, presenter: @presenter, stays_count: @stays_count, bookings: @bookings, currency_totals: @currency_totals %>
</div>

<%= turbo_frame_tag "offcanvas_drawer", data: { offcanvas_variant: "right" } do %>
  <div class="flex flex-col h-full">
    <%= form_with model: @booking, url: check_in_hotel_booking_path(current_hotel, @booking), method: :post,
        class: "flex flex-col h-full", data: { controller: "group-lifecycle-targets" } do |f| %>
      <%= hidden_field_tag :source, @transaction_source if @transaction_source.present? %>
      <%= hidden_field_tag :return_to, @transaction_return_to if @transaction_return_to.present? %>

      <%# Modal Header %>
      <%= render "hotel_portal/bookings/board/partials/content_header",
                 title: @presenter.check_in_title,
                 subtitle: "Set check-in time and assign rooms for #{@booking.guest_name}.",
                  icon: cached_icon("log-in", stroke_width: 2.5, class: "size-5") %>

      <%# Modal Content %>
      <div class="flex-1 <%= @booking.group_booking_id? ? 'grid min-h-0 grid-rows-[auto_minmax(0,1fr)] overflow-hidden lg:grid-cols-[20rem_minmax(0,1fr)] lg:grid-rows-none' : 'space-y-6 overflow-y-auto p-6 custom-scrollbar' %>">
        <% currency = current_hotel.default_currency.presence || @booking.currency.presence || "MYR" %>
        <%= render "hotel_portal/bookings/transactions/shared/group_target_selector",
          target_presenter: HotelPortal::BookingLifecycleTargetPresenter.new(booking: @booking, action: :check_in) %>

        <% if @booking.group_booking_id? %>
          <div class="min-h-0 overflow-y-auto custom-scrollbar">
            <div class="space-y-6 p-6">
        <% end %>

        <% if @booking.guarantee_method.present? && @booking.guarantee_method != "none" %>
          <%= render "hotel_portal/bookings/guarantee_status_badge", booking: @booking %>
        <% end %>

        <div>
          <label class="mb-2 block text-[11px] font-bold uppercase tracking-wider text-slate-500">Check-in Date & Time</label>
          <%= f.datetime_local_field :checked_in_at,
              value: @presenter.checked_in_at_form_value,
              class: "w-full rounded-xl border border-slate-200 bg-white py-2.5 px-4 text-sm text-slate-700 focus:border-slate-300 focus:outline-none focus:ring-0 shadow-sm" %>
        </div>

        <% unless @booking.checked_in? %>
          <div class="space-y-4 border-t border-slate-100 py-4" data-controller="payment-toggle">
            <div class="flex items-start justify-between gap-4">
              <div>
                <h3 class="text-sm font-bold text-slate-900">Security Deposit</h3>
                <p class="mt-1 text-xs font-medium text-slate-500">Record a refundable deposit separately from the guest folio.</p>
              </div>
              <label class="relative mt-0.5 inline-flex shrink-0 cursor-pointer items-center">
                <%= check_box_tag :collect_security_deposit,
                                  "1",
                                  false,
                                  class: "peer sr-only",
                                  aria: { label: "I have collected the guest's security deposit" },
                                  data: { payment_toggle_target: "checkbox", action: "change->payment-toggle#toggle" } %>
                <span class="block h-6 w-11 rounded-full bg-slate-200 transition peer-checked:bg-slate-900 peer-focus-visible:ring-2 peer-focus-visible:ring-slate-300 after:absolute after:left-[2px] after:top-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-[''] peer-checked:after:translate-x-full peer-checked:after:border-white"></span>
              </label>
            </div>

            <div class="flex items-center justify-between gap-4" data-payment-toggle-target="container">
              <%= label_tag :security_deposit_amount, "Security Deposit Amount", class: "text-[11px] font-bold uppercase tracking-wider text-slate-500" %>
              <div class="flex w-44 overflow-hidden rounded-xl border border-slate-200 bg-slate-50 text-sm font-black text-slate-900 shadow-sm">
                <span class="flex items-center border-r border-slate-200 px-3 text-xs font-bold text-slate-500"><%= currency %></span>
                <%= number_field_tag :security_deposit_amount,
                                     "0.00",
                                     step: 0.01,
                                     min: 0,
                                     disabled: true,
                                     class: "min-w-0 flex-1 border-0 bg-slate-50 px-3 py-2 text-right font-black text-slate-900 focus:outline-none focus:ring-0 disabled:cursor-not-allowed disabled:text-slate-400",
                                     data: { payment_toggle_target: "field" } %>
              </div>
            </div>

            <div class="grid gap-3 sm:grid-cols-2" data-payment-toggle-target="container">
              <div>
                <%= label_tag :security_deposit_payment_method, "Method", class: "mb-2 block text-[11px] font-bold uppercase tracking-wider text-slate-500" %>
                <%= select_tag :security_deposit_payment_method, options_for_select([["Cash", "cash"], ["Credit Card", "credit_card"], ["Bank Transfer", "bank_transfer"], ["Manual", "manual"]], "cash"), disabled: true, class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-400", data: { payment_toggle_target: "field" } %>
              </div>
              <div>
                <%= label_tag :security_deposit_reference, "Reference", class: "mb-2 block text-[11px] font-bold uppercase tracking-wider text-slate-500" %>
                <%= text_field_tag :security_deposit_reference, nil, placeholder: "Optional receipt/reference", disabled: true, class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-400", data: { payment_toggle_target: "field" } %>
              </div>
            </div>
          </div>

          <% tourism_tax_total = @booking.tourism_tax_total %>
          <% if tourism_tax_total.positive? %>
            <div class="space-y-4 border-t border-slate-100 py-4">
              <div>
                <h3 class="text-sm font-bold text-slate-900">Tourism Tax</h3>
                <p class="mt-1 text-xs font-medium text-slate-500">Record collection of mandatory tourism tax.</p>
              </div>

              <div class="flex items-center justify-between gap-4">
                <span class="text-[11px] font-bold uppercase tracking-wider text-slate-500">Total Tourism Tax</span>
                <div class="flex w-36 overflow-hidden rounded-xl border border-slate-200 bg-slate-50 text-sm font-black text-slate-900 shadow-sm">
                  <span class="flex items-center border-r border-slate-200 px-3 text-xs font-bold text-slate-500"><%= currency %></span>
                  <%= text_field_tag :tourism_tax_amount,
                                     number_with_precision(tourism_tax_total, precision: 2),
                                     disabled: true,
                                     class: "min-w-0 flex-1 border-0 bg-slate-50 px-3 py-2 text-right font-black text-slate-900 focus:outline-none focus:ring-0 disabled:cursor-not-allowed" %>
                </div>
              </div>

              <div class="flex items-start justify-between gap-4">
                <div>
                  <p class="text-sm font-bold text-slate-900">I have collected the guest's total tourism tax</p>
                  <p class="mt-1 text-xs font-medium leading-5 text-slate-500">If this is off, tourism tax will remain unpaid on the guest folio and may appear as an outstanding balance after tax charges are posted.</p>
                </div>
                <label class="relative mt-0.5 inline-flex shrink-0 cursor-pointer items-center">
                  <%= f.check_box :tourism_tax_collected,
                                  class: "peer sr-only",
                                  aria: { label: "I have collected the guest's total tourism tax" } %>
                  <span class="block h-6 w-11 rounded-full bg-slate-200 transition peer-checked:bg-slate-900 peer-focus-visible:ring-2 peer-focus-visible:ring-slate-300 after:absolute after:left-[2px] after:top-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-[''] peer-checked:after:translate-x-full peer-checked:after:border-white"></span>
                </label>
              </div>
            </div>
          <% end %>
        <% end %>

        <div class="space-y-3">
          <label class="mb-2 block text-[11px] font-bold uppercase tracking-wider text-slate-500">Room Assignments</label>
          <%= f.fields_for :booking_rooms do |rf| %>
            <div class="flex items-center gap-3"
                 data-controller="room-lock room-selection"
                 data-room-lock-url-value="<%= hotel_room_locks_path(current_hotel) %>"
                 data-room-lock-room-type-id-value="<%= rf.object.room_type_id %>"
                 data-room-lock-current-room-value="<%= rf.object.room_number %>"
                 data-room-selection-url-value="<%= availability_hotel_bookings_path(current_hotel) %>"
                 data-room-selection-room-type-id-value="<%= rf.object.room_type_id %>"
                 data-room-selection-check-in-value="<%= @booking.check_in.to_date %>"
                 data-room-selection-check-out-value="<%= @booking.check_out.to_date %>"
                 data-room-selection-booking-id-value="<%= @booking.id %>"
                 data-room-selection-current-room-value="<%= rf.object.room_number %>">
              <div class="flex-1">
                <p class="text-[10px] font-medium text-slate-400 uppercase mb-1"><%= rf.object.room_type.name %></p>
                <%= rf.select :room_number, [[rf.object.room_number, rf.object.room_number]], { selected: rf.object.room_number },
                    class: "w-full rounded-xl border border-slate-200 bg-white py-2.5 px-4 text-sm text-slate-700 focus:border-slate-300 focus:outline-none focus:ring-0 shadow-sm",
                    data: {
                      room_selection_target: "select",
                      action: "change->room-lock#lock"
                    } %>
                <p class="text-[10px] text-slate-400 mt-1.5 italic">Only available rooms for the stay dates are shown.</p>
              </div>
            </div>
          <% end %>
        </div>

        <% if @presenter.requires_backdated_checkin_reason? %>
          <div class="mt-4 pt-4 border-t border-slate-100">
            <div class="flex gap-3">
              <div class="flex-shrink-0 mt-0.5">
                <%= cached_icon "triangle-alert", stroke_width: 2, class: "size-4 text-orange-500" %>
              </div>
              <div class="flex-1">
                <h4 class="text-xs font-bold text-slate-900">Backdated Check-in</h4>
                <p class="text-[11px] text-slate-500 mt-1 mb-3">Night audit has already run for this check-in date. This operation is for unexpected arrivals after the audit.</p>

                <div class="space-y-3">
                  <label class="flex items-center gap-2">
                    <%= check_box_tag :override_night_audit, "1", false, class: "rounded text-slate-900 focus:ring-slate-900 border-slate-300" %>
                    <span class="text-[11px] font-bold text-slate-700 uppercase tracking-wider">Override Night Audit</span>
                  </label>

                  <div>
                    <%= text_field_tag :retroactive_reason, nil, placeholder: "Provide a reason (e.g. system offline, missed check-in)", class: "w-full rounded-xl border border-slate-200 bg-white py-2.5 px-4 text-sm text-slate-700 focus:border-slate-300 focus:outline-none focus:ring-0 shadow-sm" %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <% if @booking.group_booking_id? %>
            </div>
          </div>
        <% end %>
      </div>

      <%# Modal Footer %>
      <%= render "hotel_portal/bookings/board/partials/content_footer",
                 show_full_page_link: false,
                 submit_button: f.submit((@booking.checked_in? ? "Save Changes" : "Confirm Check In"), class: "px-6 py-2 bg-slate-900 text-white rounded-xl text-sm font-bold hover:bg-slate-700 transition-colors shadow-sm") %>
    <% end %>
  </div>
<% end %>

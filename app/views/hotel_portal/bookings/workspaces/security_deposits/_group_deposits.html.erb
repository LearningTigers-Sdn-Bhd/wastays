<section class="border-t border-border" aria-labelledby="group-deposits-heading">
  <h3 id="group-deposits-heading" class="border-b border-border px-5 py-3 text-sm font-bold text-foreground">Group Deposits</h3>
  <div class="overflow-x-auto">
    <table class="w-full min-w-[720px] text-left text-sm">
      <thead class="border-b border-border bg-muted text-xs font-semibold text-muted-foreground">
        <tr><th class="px-5 py-3">Reference</th><th class="px-5 py-3">Received</th><th class="px-5 py-3">Method</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Amount</th><th class="px-5 py-3 text-right">Available</th></tr>
      </thead>
      <tbody class="divide-y divide-border">
        <% if presenter.group_booking.group_deposits.any? %>
          <% presenter.group_booking.group_deposits.each do |deposit| %>
            <tr><td class="px-5 py-3 font-semibold text-foreground"><%= deposit.external_reference.presence || "—" %></td><td class="px-5 py-3 text-foreground"><%= presenter.time_label(deposit.received_at) %></td><td class="px-5 py-3 text-foreground"><%= deposit.payment_method.humanize %></td><td class="px-5 py-3 text-foreground"><%= deposit.status.humanize %></td><td class="px-5 py-3 text-right font-semibold tabular-nums text-foreground"><%= presenter.money(deposit.amount) %></td><td class="px-5 py-3 text-right font-semibold tabular-nums text-foreground"><%= presenter.money(deposit.available_amount) %></td></tr>
          <% end %>
        <% else %>
          <tr><td colspan="6" class="px-5 py-10 text-center text-muted-foreground">No group deposits have been received.</td></tr>
        <% end %>
      </tbody>
    </table>
  </div>
</section>

<div class="space-y-5" data-controller="payment-toggle">
  <p class="text-xs font-bold uppercase tracking-wider text-muted-foreground">Payment</p>
  <%= render PanelsUI::Switch.new(
        form: f,
        attribute: :record_payment,
        checked: false,
        label: "Record payment now",
        description: "Otherwise payment remains pending.",
        data: { action: "change->payment-toggle#toggle", payment_toggle_target: "checkbox" }) %>

  <div class="space-y-4 border-t border-border pt-5 opacity-50" data-payment-toggle-target="container">
    <%# payment-toggle disables the wrapper's inner <select> AND trigger <button>,
        which is what gates the SelectMenu, so the runtime enable/disable works. %>
    <%= render PanelsUI::FormField.new(form: f, attribute: :payment_method, label: "Payment method") do |field| %>
      <% field.with_select_menu(
           [["Cash", "cash"], ["Credit Card", "credit_card"], ["Bank Transfer", "bank_transfer"]],
           selected: "cash",
           disabled: true,
           data: { payment_toggle_target: "field" }) %>
    <% end %>
    <%= render PanelsUI::FormField.new(form: f, attribute: :payment_amount, label: "Amount (MYR)", hint: "Defaults to the total if left blank.", disabled: true) do |field| %>
      <% field.with_input(type: :number, step: 0.01, data: { booking_calc_target: "paymentAmountInput", payment_toggle_target: "field" }) %>
    <% end %>
    <%= render PanelsUI::FormField.new(form: f, attribute: :payment_reference, label: "Reference / receipt no.", disabled: true) do |field| %>
      <% field.with_input(placeholder: "Optional reference", data: { payment_toggle_target: "field" }) %>
    <% end %>
  </div>
</div>

<div>
  <%= label_tag :retroactive_reason, "Reason for reinstatement", class: "mb-2 block text-[11px] font-bold uppercase tracking-wider text-muted-foreground" %>
  <%= text_area_tag :retroactive_reason,
        params[:retroactive_reason].presence || "Guest arrived late after night audit.",
        required: true,
        rows: 4,
        class: "w-full rounded-xl border border-border bg-card px-4 py-3 text-sm text-foreground focus:border-ring focus:ring-ring" %>
</div>

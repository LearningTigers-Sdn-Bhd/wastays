<%# LEGACY: frozen pending booking-control-panel migration. Do not add features here. %>
<%= render "hotel_portal/folios/show/page",
  booking: @booking,
  presenter: @presenter,
  folio_show: @folio_show,
  back_path: @folio_back_path,
  back_label: @folio_back_label,
  folio_origin: @folio_origin %>

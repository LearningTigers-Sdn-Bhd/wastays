<% folio = booking.booking_folio %>
<% currency = booking.currency.presence || "MYR" %>
<% projected_lines = Array(local_assigns[:projected_lines]) %>
<% projected_charges = projected_lines.sum { |line| line[:amount].to_d } %>
<% total_charges = (folio&.total_charges || 0).to_d + projected_charges %>
<% total_payments = (folio&.total_payments || 0).to_d %>
<% total_adjustments = (folio&.total_adjustments || 0).to_d %>
<% outstanding_balance = total_charges - total_payments + total_adjustments %>

<div class="bg-primary rounded-3xl p-8 text-primary-foreground shadow-xl shadow-foreground/50">
  <div class="flex flex-col md:flex-row md:items-center justify-between gap-8">
    <div>
      <div class="flex items-center gap-2 mb-4">
        <h3 class="text-[11px] font-black text-muted-foreground uppercase tracking-[0.2em]">Outstanding Balance</h3>
        <div class="size-5 rounded-lg bg-card/10 flex items-center justify-center text-primary-foreground">
          <%= cached_icon "credit-card", stroke_width: 2.5, class: "size-3" %>
        </div>
      </div>
      <div class="flex items-baseline gap-2">
        <span class="text-sm font-bold text-muted-foreground"><%= currency %></span>
        <span class="text-5xl font-black tracking-tight"><%= format_number(outstanding_balance, precision: 2) %></span>
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-8 md:gap-12 md:pl-12 md:border-l border-border-interactive">
      <div>
        <p class="text-[10px] font-bold text-muted-foreground uppercase tracking-widest mb-1">Folio Status</p>
        <p class="text-lg font-black uppercase tracking-tight text-primary-foreground"><%= folio&.status || "N/A" %></p>
      </div>
      <div>
        <p class="text-[10px] font-bold text-muted-foreground uppercase tracking-widest mb-1">Total Charges</p>
        <p class="text-lg font-black text-primary-foreground"><%= currency %> <%= format_number(total_charges, precision: 2) %></p>
      </div>
      <div>
        <p class="text-[10px] font-bold text-muted-foreground uppercase tracking-widest mb-1">Total Payments</p>
        <p class="text-lg font-black text-emerald-400"><%= currency %> <%= format_number(total_payments, precision: 2) %></p>
      </div>
    </div>
  </div>
</div>

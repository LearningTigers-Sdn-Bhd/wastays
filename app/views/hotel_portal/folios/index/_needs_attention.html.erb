<section class="mb-4 overflow-hidden rounded-md border border-slate-200 bg-white shadow-sm">
  <div class="flex items-center justify-between border-b border-slate-200 bg-slate-50/70 px-4 py-3">
    <h2 class="inline-flex items-center gap-2 text-sm font-semibold text-slate-950">
      <span class="inline-flex size-6 items-center justify-center rounded-md bg-amber-50 text-amber-700">
        <%= cached_icon "triangle-alert", class: "size-3.5" %>
      </span>
      Needs Attention
    </h2>
    <% if folio_index.attention_rows.size == 5 && !folio_index.show_all_attention? %>
      <%= link_to hotel_folios_path(current_hotel, attention: "all"), class: "text-xs font-bold text-slate-500 transition hover:text-slate-900", data: { turbo_frame: "_top" } do %>
        View all &rsaquo;
      <% end %>
    <% end %>
  </div>

  <% if folio_index.visible_attention_rows.any? %>
    <div class="divide-y divide-slate-100">
      <% folio_index.visible_attention_rows.each do |row| %>
        <article class="grid gap-3 px-4 py-3 lg:grid-cols-[minmax(0,1.6fr)_minmax(0,1.4fr)_minmax(0,0.9fr)_minmax(0,1fr)_auto] lg:items-center">
          <div class="flex min-w-0 items-start gap-3">
            <span class="mt-0.5 inline-flex size-7 shrink-0 items-center justify-center rounded-md bg-amber-50 text-amber-700">
              <%= cached_icon row.attention_icon, stroke_width: 2.25, class: "size-4" %>
            </span>
            <div class="min-w-0">
              <p class="truncate text-sm font-black text-slate-950"><%= row.booking.guest_name %></p>
              <p class="mt-0.5 text-xs font-medium text-slate-500"><%= row.booking.confirmation_token %> · <%= row.folio_reference %></p>
              <p class="mt-1 text-xs font-semibold text-slate-600"><%= row.attention_reason %> · <%= row.folio.status.humanize %> folio</p>
            </div>
          </div>

          <div class="text-xs font-bold text-slate-600"><%= row.room_label %></div>
          <div class="text-sm font-black tabular-nums text-slate-950"><%= row.amount_label %></div>
          <div class="flex flex-wrap gap-1.5">
            <% row.status_badges.first(3).each do |badge| %>
              <span class="inline-flex items-center gap-1 rounded-md border px-2 py-0.5 text-[10px] font-black uppercase tracking-wider <%= badge[:class] %>">
                <%= cached_icon badge[:icon], class: "size-3" %>
                <%= badge[:label] %>
              </span>
            <% end %>
          </div>
          <div class="flex items-center gap-2 lg:justify-end">
            <%= link_to hotel_booking_control_panel_path(current_hotel, row.booking, tab: "folio_operations", folio_id: row.folio.id), class: "inline-flex items-center gap-1.5 rounded-lg bg-slate-900 px-3 py-1.5 text-xs font-black text-white shadow-sm transition hover:bg-slate-700", data: { turbo_frame: "_top" } do %>
              <%= cached_icon "book-open", class: "size-3" %>
              View Folio
            <% end %>
            <%= link_to hotel_booking_control_panel_path(current_hotel, row.booking, tab: "booking_details"), class: "inline-flex items-center gap-1.5 rounded-lg border border-slate-200 bg-white px-3 py-1.5 text-xs font-black text-slate-700 shadow-sm transition hover:border-slate-900 hover:bg-slate-50", data: { turbo_frame: "_top" } do %>
              <%= cached_icon "calendar-days", class: "size-3" %>
              View Booking
            <% end %>
          </div>
        </article>
      <% end %>
    </div>
  <% else %>
    <div class="px-4 py-8 text-center">
      <p class="text-sm font-bold text-slate-900">No folios need attention.</p>
      <p class="mt-1 text-xs font-medium text-slate-500">Balances, due-out folios, and refund syncs look clear for this view.</p>
    </div>
  <% end %>
</section>

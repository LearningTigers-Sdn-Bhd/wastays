<% if folio_index.pagination.pages > 1 %>
  <div class="mt-6 flex justify-center">
    <%= render PanelsUI::Pagination.new(pagy: folio_index.pagination, hide_when_single_page: true) %>
  </div>
<% end %>

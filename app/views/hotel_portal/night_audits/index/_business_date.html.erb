<section class="overflow-hidden rounded-xl border border-border bg-card shadow-sm" data-testid="business-date-card">
  <header class="flex items-center justify-between gap-3 border-b border-border bg-muted/60 px-4 py-2.5">
    <h2 class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-foreground">
      <span class="inline-flex size-6 items-center justify-center rounded-md bg-blue-50 text-blue-700"><%= cached_icon "calendar-days", class: "size-3.5", aria: { hidden: true } %></span>
      Business Date
    </h2>
    <div class="flex flex-wrap items-center justify-end gap-2">
      <span class="text-xs font-semibold text-muted-foreground">Accounting authority</span>
      <span class="inline-flex items-center rounded-md border px-2 py-0.5 text-[11px] font-semibold uppercase tracking-wide <%= presenter.business_date_status_badge_class %>" data-testid="business-date-status">
        <%= presenter.business_date_status_label %>
      </span>
    </div>
  </header>
  <div class="grid gap-px bg-muted md:grid-cols-[1fr_1fr_0.8fr]">
    <div class="min-w-0 bg-card px-4 py-2.5">
      <p class="text-[10px] font-bold uppercase tracking-wider text-muted-foreground">Current Business Date</p>
      <p class="mt-0.5 text-sm font-semibold text-foreground"><%= presenter.current_business_date_label %></p>
      <p class="mt-0.5 text-xs text-muted-foreground">Authoritative audit target</p>
    </div>
    <div class="min-w-0 bg-card px-4 py-2.5">
      <p class="text-[10px] font-bold uppercase tracking-wider text-muted-foreground">Calendar Date</p>
      <p class="mt-0.5 text-sm font-semibold text-foreground"><%= presenter.calendar_date_label %></p>
      <p class="mt-0.5 text-xs text-muted-foreground">Hotel calendar date</p>
    </div>
    <div class="min-w-0 bg-card px-4 py-2.5">
      <p class="text-[10px] font-bold uppercase tracking-wider text-muted-foreground">Timezone</p>
      <p class="mt-0.5 text-sm font-semibold text-foreground"><%= presenter.hotel_timezone_label %></p>
      <p class="mt-0.5 text-xs text-muted-foreground">Hotel-local time</p>
    </div>
    <div class="bg-card px-4 py-2.5 md:col-span-3 <%= presenter.date_difference? ? 'text-amber-900' : 'text-foreground' %>">
      <p class="text-xs font-medium"><%= presenter.date_explanation %></p>
    </div>
  </div>
</section>

<% presenter = @presenter %>

<% if presenter.auto_refresh? %>
  <% content_for :head do %>
    <meta http-equiv="refresh" content="5">
  <% end %>
<% end %>

<div class="mx-auto max-w-[1600px] space-y-4 px-4 pb-16 pt-5 md:px-0">
  <%= render "hotel_portal/night_audits/show/header", presenter: presenter %>
  <%= render "hotel_portal/night_audits/show/result_banner", presenter: presenter %>
  <%= render "hotel_portal/night_audits/show/summary", presenter: presenter %>

  <div data-controller="tabs" data-tabs-default-tab-value="results" class="space-y-4">
    <%= render "hotel_portal/night_audits/show/tabs", presenter: presenter %>

    <div id="results-panel" data-tabs-target="panel" data-tab-panel="results" role="tabpanel" class="space-y-4" data-testid="results-panel">
      <%= render "hotel_portal/night_audits/show/run_results", presenter: presenter %>
      <%= render "hotel_portal/night_audits/show/blockers_and_warnings", presenter: presenter %>
    </div>

    <div id="financial-summary-panel" data-tabs-target="panel" data-tab-panel="financial-summary" role="tabpanel" class="hidden" data-testid="financial-summary-panel">
      <%= render "hotel_portal/night_audits/show/financial_summary", presenter: presenter %>
    </div>

    <div id="advanced-actions-panel" data-tabs-target="panel" data-tab-panel="advanced-actions" role="tabpanel" class="hidden" data-testid="show-advanced-actions-panel">
      <%= render "hotel_portal/night_audits/show/manual_adjustments", presenter: presenter %>
    </div>
  </div>
</div>

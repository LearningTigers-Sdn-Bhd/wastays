<div class="room-status-board <%= 'room-status-comfortable' if presenter.comfortable_mode? %> overflow-hidden rounded-2xl border border-border bg-card bg-[repeating-linear-gradient(-45deg,transparent,transparent_10px,#f8fafc_10px,#f8fafc_20px)] shadow-sm">
  <% if presenter.room_groups.blank? %>
    <div class="px-8 py-20 text-center bg-card">
      <p class="text-sm font-medium text-muted-foreground">No rooms found for this property.</p>
    </div>
  <% else %>
    <div class="overflow-x-auto">
      <div class="min-w-full w-fit">
        <div class="grid border-b border-border bg-muted" style="grid-template-columns: <%= presenter.grid_template_columns %>;">
          <div class="sticky left-0 z-30 border-r border-border bg-muted shadow-[4px_0_12px_rgba(0,0,0,0.03)] <%= presenter.card_padding %> flex items-center pl-3 text-sm font-semibold uppercase tracking-[0.08em] text-muted-foreground">
            Rooms
          </div>

          <% presenter.board_dates.each_with_index do |date, index| %>
            <div class="border-l <%= 'border-r' if index == presenter.board_dates.size - 1 %> border-border <%= presenter.summary_padding %> text-center flex flex-col justify-center <%= 'bg-muted' if date == Date.current %>">
              <p class="text-[9px] font-bold uppercase tracking-widest text-muted-foreground mb-0.5"><%= date.strftime('%a') %></p>
              <p class="<%= presenter.comfortable_mode? ? 'text-sm' : 'text-xs' %> font-medium leading-none text-foreground whitespace-nowrap"><%= date.strftime('%-d %b') %></p>
            </div>
          <% end %>
        </div>

        <% presenter.room_groups.each do |group| %>
          <details open class="group border-b border-border">
            <summary class="list-none cursor-pointer bg-muted [&::-webkit-details-marker]:hidden">
              <div class="grid" style="grid-template-columns: <%= presenter.grid_template_columns %>;">
                <div class="sticky left-0 z-20 bg-muted text-[10px] font-bold uppercase tracking-[0.08em] text-muted-foreground">
                  <div class="flex items-center justify-between gap-2 opacity-0 <%= presenter.comfortable_mode? ? 'px-4 py-2' : 'px-3 py-1' %> pointer-events-none">
                    <span class="truncate whitespace-nowrap"><%= group[:room_type].name %></span>
                    <svg class="size-4 shrink-0 text-muted-foreground transition-transform duration-150 group-open:rotate-90" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <path d="m9 6 6 6-6 6"/>
                    </svg>
                  </div>
                  <div class="absolute inset-y-0 left-0 w-max min-w-full flex items-center justify-between gap-4 <%= presenter.comfortable_mode? ? 'px-4 py-2' : 'px-3 py-1' %> pointer-events-auto">
                    <span class="whitespace-nowrap"><%= group[:room_type].name %></span>
                    <svg class="size-4 shrink-0 text-muted-foreground transition-transform duration-150 group-open:rotate-90" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <path d="m9 6 6 6-6 6"/>
                    </svg>
                  </div>
                </div>
                <div style="grid-column: 2 / -1;" class="bg-muted border-r border-border"></div>
              </div>
            </summary>

            <% if group[:rooms].blank? %>
              <div class="grid min-h-[72px] border-t border-border" style="grid-template-columns: <%= presenter.grid_template_columns %>;">
                <div class="flex items-center justify-center gap-2 px-6 py-4 text-center border-r border-border bg-card" style="grid-column: 1 / -1;">
                  <p class="text-sm font-semibold text-muted-foreground">No rooms configured.</p>
                  <p class="text-sm font-medium text-muted-foreground">Add room numbers in Room Categories to start tracking readiness for this room type.</p>
                </div>
              </div>
            <% end %>

            <% group[:rooms].each do |room| %>
              <%= render "hotel_portal/room_status_board/room_row", room: room, presenter: presenter %>
            <% end %>
          </details>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

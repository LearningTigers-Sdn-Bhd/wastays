<div class="flex flex-col items-center justify-center px-4 py-10 text-center">
  <div class="mb-3 rounded-full bg-muted p-3 text-muted-foreground">
    <%= cached_icon "file-search", stroke_width: 2, class: "size-6" %>
  </div>
  <h3 class="text-sm font-bold text-foreground">No AR invoices found</h3>
  <p class="mt-1 max-w-[300px] text-xs text-muted-foreground">Try adjusting the search, corporate account, due date, or status filters.</p>
  <% if presenter.filters_active? %>
    <%= link_to hotel_ar_invoices_path(current_hotel), class: "mt-4 inline-flex items-center gap-1.5 rounded-lg border border-border bg-card px-3 py-1.5 text-xs font-bold text-foreground shadow-sm transition hover:bg-muted active:scale-95", data: { turbo_frame: "_top" } do %>
      <%= cached_icon "rotate-ccw", stroke_width: 2.5, class: "size-3" %>
      Reset Filters
    <% end %>
  <% end %>
</div>

<section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm" data-testid="payment-allocations-card">
  <header class="flex items-center justify-between border-b border-slate-200 bg-slate-50/60 px-4 py-2.5">
    <h2 class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-slate-700">
      <span class="inline-flex size-6 items-center justify-center rounded bg-emerald-50 text-emerald-700">
        <%= cached_icon "credit-card", class: "size-3.5", aria: { hidden: true } %>
      </span>
      Payment Allocations
    </h2>
  </header>

  <% if presenter.allocation_rows.any? %>
    <div class="overflow-x-auto">
      <table class="min-w-[720px] w-full text-left text-sm">
        <thead class="border-b border-slate-200 bg-slate-50 text-[10px] font-bold uppercase tracking-wider text-slate-500">
          <tr>
            <th scope="col" class="whitespace-nowrap px-4 py-3">Received</th>
            <th scope="col" class="whitespace-nowrap px-4 py-3">Reference</th>
            <th scope="col" class="whitespace-nowrap px-4 py-3">Method</th>
            <th scope="col" class="whitespace-nowrap px-4 py-3 text-right">Allocated</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
          <% presenter.allocation_rows.each do |allocation| %>
            <tr class="transition hover:bg-slate-50/70">
              <td class="whitespace-nowrap px-4 py-3 text-slate-600"><%= allocation.received_on %></td>
              <th scope="row" class="whitespace-nowrap px-4 py-3 font-semibold text-slate-950"><%= allocation.reference %></th>
              <td class="whitespace-nowrap px-4 py-3 text-slate-600"><%= allocation.payment_method %></td>
              <td class="whitespace-nowrap px-4 py-3 text-right font-bold tabular-nums text-slate-950"><%= allocation.allocated_amount %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="px-6 py-12 text-center">
      <span class="mx-auto inline-flex size-10 items-center justify-center rounded-full bg-slate-100 text-slate-500">
        <%= cached_icon "credit-card", class: "size-5", aria: { hidden: true } %>
      </span>
      <p class="mt-3 text-sm font-semibold text-slate-700">No payment allocations yet</p>
      <p class="mt-1 text-xs text-slate-500">Received payments allocated to this invoice will appear here.</p>
    </div>
  <% end %>
</section>

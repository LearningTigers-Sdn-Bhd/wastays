<div class="max-w-[1600px] mx-auto pb-10 space-y-3.5">
  <div>
      <%= link_to "Back", hotel_portal_agent_accounts_path, class: "text-sm font-medium text-slate-500 hover:text-slate-700" %>
      <%= render PanelsUI::PageHeader.new(title: "Edit Agent Account", description: "Update agent account details.") do |header| %>
        <% header.with_caption do %>
          <%= @agent_account.name %>
        <% end %>
      <% end %>
  </div>

  <div class="card p-6">
    <%= render "form", agent_account: @agent_account %>
  </div>
</div>

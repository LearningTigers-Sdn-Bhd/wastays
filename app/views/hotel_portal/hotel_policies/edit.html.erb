<% content_for :breadcrumbs do %>
  <div class="flex items-center gap-x-2">
    <span class="text-sm font-medium text-slate-600"><%= current_hotel.name %></span>
    <span class="text-sm text-slate-400">/</span>
    <span class="text-sm font-semibold text-slate-900">Hotel Policy</span>
  </div>
<% end %>

<div class="max-w-[1600px] mx-auto pb-20 space-y-8">
  <%= form_with model: @hotel, url: hotel_policy_path(@hotel), method: :patch, local: true, id: "hotel-policy-form", data: { controller: "hotel-policy", hotel_policy_initial_value: @hotel.policy.to_json } do |f| %>
    <div class="px-4 md:px-0">
      <div class="mt-5 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between">
        <div>
          <h1 class="text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Hotel Policy</h1>
          <p class="mt-1 max-w-2xl text-sm font-medium leading-6 text-slate-600">Structure your key stay rules and expectations into clear policy blocks guests can understand quickly.</p>
        </div>

        <div class="flex items-center gap-3">
          <button type="button"
                  data-action="click->hotel-policy#addItem"
                  class="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-slate-300 hover:bg-slate-50">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
            Add Policy
          </button>
        </div>
      </div>
    </div>

    <div class="px-4 md:px-0">
      <div class="bg-slate-50/30 py-4 sm:py-6 lg:py-8">
        <%= f.hidden_field :policy, data: { hotel_policy_target: "hiddenInput" } %>

        <div data-hotel-policy-target="container" class="space-y-4"></div>
      </div>
    </div>
  <% end %>
</div>

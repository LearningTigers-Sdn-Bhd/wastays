Financial Observability Daily Digest
====================================

Hotel: <%= @hotel.name %>
Date: <%= Time.current.strftime("%B %d, %Y") %>

<% if @anomalies[:unbalanced_folios].any? %>
Unbalanced Folios (Closed)
--------------------------
<% @anomalies[:unbalanced_folios].each do |folio| %>
* <%= folio[:confirmation_token] %> (<%= folio[:guest_name] %>): <%= number_to_currency(folio[:balance]) %>
<% end %>
<% else %>
No unbalanced closed folios detected.
<% end %>

<% if @anomalies[:audit_sync_lags].any? %>
Audit Sync Lags
---------------
<% @anomalies[:audit_sync_lags].each do |lag| %>
* <%= lag[:business_date] %>: <%= lag[:status].titleize %> (<%= lag[:lag_days] %> days behind)
<% end %>
<% else %>
Audit is up to date.
<% end %>

<% if @anomalies[:override_abuse] %>
Closed-Date Override Warning
----------------------------
There have been <%= @anomalies[:override_abuse][:count] %> closed-date overrides in the last 24 hours.
Latest Actors: <%= @anomalies[:override_abuse][:latest_actors].join(", ") %>
<% else %>
Closed-date override usage within normal range.
<% end %>

---
This is an automated alert generated by Wastays Financial Observability.
Frequency: <%= distance_of_time_in_words(@team_config.frequency) %>

<% content_for :title, "Dashboard | WAStays" %>
<% content_for :breadcrumb_current, "Dashboard" %>

<div class="space-y-6">
  <!-- Header -->
  <div class="space-y-1">
    <h1 class="text-2xl font-semibold text-neutral-text-primary">Dashboard</h1>
    <p class="text-sm text-neutral-text-secondary">Welcome back, <%= current_guest.name.split.first %>. Here's an overview of your stays.</p>
  </div>

  <!-- Stats Grid -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
    <div class="bg-white rounded-2xl border border-layer-line p-5 shadow-sm">
      <p class="text-[10px] font-bold uppercase tracking-wider text-neutral-text-secondary mb-1">Total Bookings</p>
      <div class="flex items-end gap-x-2">
        <span class="text-2xl font-bold text-neutral-text-primary"><%= @total_bookings %></span>
      </div>
    </div>
    <div class="bg-white rounded-2xl border border-layer-line p-5 shadow-sm">
      <p class="text-[10px] font-bold uppercase tracking-wider text-neutral-text-secondary mb-1">Upcoming Stays</p>
      <div class="flex items-end gap-x-2">
        <span class="text-2xl font-bold text-neutral-text-primary"><%= @upcoming_bookings.count %></span>
      </div>
    </div>
  </div>

  <!-- Recent Bookings Table-style Card -->
  <div class="bg-white rounded-2xl border border-layer-line shadow-sm overflow-hidden">
    <div class="px-6 py-4 border-b border-layer-line flex items-center justify-between">
      <h2 class="font-semibold text-neutral-text-primary">Recent Bookings</h2>
      <%= link_to "View All Bookings", guest_bookings_path, class: "text-xs font-bold text-brand-primary hover:underline" %>
    </div>
    
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-layer-line">
        <thead class="bg-layer-alt">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-[10px] font-bold text-neutral-text-secondary uppercase tracking-wider">Hotel</th>
            <th scope="col" class="px-6 py-3 text-left text-[10px] font-bold text-neutral-text-secondary uppercase tracking-wider">Stay Dates</th>
            <th scope="col" class="px-6 py-3 text-left text-[10px] font-bold text-neutral-text-secondary uppercase tracking-wider">Status</th>
            <th scope="col" class="px-6 py-3 text-right text-[10px] font-bold text-neutral-text-secondary uppercase tracking-wider">Action</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-layer-line">
          <% if @upcoming_bookings.any? || @past_bookings.any? %>
            <% (@upcoming_bookings + @past_bookings).first(5).each do |booking| %>
              <tr class="hover:bg-layer-alt/50 transition-colors group">
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-semibold text-neutral-text-primary group-hover:text-brand-primary transition-colors">
                    <%= booking.hotel.name %>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-neutral-text-secondary">
                    <%= booking.check_in.strftime("%d %b") %> – <%= booking.check_out.strftime("%d %b %Y") %>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                    <%= booking.status == 'confirmed' ? 'bg-green-100 text-green-700' : 'bg-neutral-100 text-neutral-600' %>">
                    <%= booking.status.humanize %>
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-right">
                  <%= link_to guest_booking_path(booking), class: "inline-flex items-center gap-x-1.5 px-3 py-1.5 rounded-lg border border-layer-line bg-white text-xs font-bold text-neutral-text-primary hover:bg-layer-alt transition-all shadow-sm" do %>
                    <svg class="size-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                    View
                  <% end %>
                </td>
              </tr>
            <% end %>
          <% else %>
            <tr>
              <td colspan="4" class="px-6 py-10 text-center text-sm text-neutral-text-secondary">
                No bookings found.
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>

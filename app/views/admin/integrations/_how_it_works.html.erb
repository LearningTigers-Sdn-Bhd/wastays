<section class="rounded-lg border border-border bg-card shadow-sm">
  <div class="border-b border-border bg-muted/80 px-5 py-4">
    <h2 class="text-base font-bold text-foreground">How It Works</h2>
    <p class="mt-1 text-sm font-medium leading-6 text-muted-foreground">Global integrations are stored as encrypted app config values.</p>
  </div>

  <div class="space-y-4 p-5">
    <p class="text-sm leading-6 text-muted-foreground">
      Outgoing webhooks are managed in
      <%= link_to "Webhooks management", admin_webhook_endpoints_path, class: "font-semibold text-foreground underline underline-offset-4" %>.
    </p>

    <div class="space-y-3">
      <% [
        [ "1", "Configure", "Save provider credentials and endpoints once for the platform." ],
        [ "2", "Connect", "Services read encrypted values from AppConfig at runtime." ],
        [ "3", "Verify", "Use connection tests or focused tools like Observation Deck to confirm setup." ]
      ].each do |number, title, body| %>
        <div class="rounded-lg border border-border bg-card p-4">
          <div class="flex items-start gap-3">
            <span class="inline-flex size-6 shrink-0 items-center justify-center rounded-full bg-primary text-xs font-bold text-primary-foreground"><%= number %></span>
            <div>
              <p class="text-sm font-bold text-foreground"><%= title %></p>
              <p class="mt-1 text-sm leading-6 text-muted-foreground"><%= body %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>

<%# The test posts the whole form, so it tries the values on screen rather than
    the saved ones. The button sits beside Save for that reason. %>
<div class="space-y-3">
  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    <%= render PanelsUI::Button.new(
          label: "Test connection",
          variant: :secondary,
          data: {
            connection_test_target: "button",
            action: "click->connection-test#run"
          }) %>
    <%= render PanelsUI::Button.new(label: save_label, type: "submit") %>
  </div>

  <p class="text-sm text-muted-foreground"
     role="status"
     aria-live="polite"
     data-connection-test-target="status"></p>
</div>

<div class="w-full max-w-3xl space-y-6">
  <%= render PanelsUI::PageHeader.new(title: "Edit Booking Source", description: "Changes apply everywhere this source is shown, across every hotel.") %>

  <section class="rounded-2xl border border-border bg-card p-6 shadow-sm">
    <%= render "form" %>
  </section>
</div>

<div class="w-full max-w-3xl space-y-6">
  <%= render PanelsUI::PageHeader.new(title: "Add Booking Source", description: "Custom sources become selectable across every hotel's booking-source dropdowns immediately.") %>

  <section class="rounded-2xl border border-border bg-card p-6 shadow-sm">
    <%= render "form" %>
  </section>
</div>

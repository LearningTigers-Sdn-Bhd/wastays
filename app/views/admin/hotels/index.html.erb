<div class="space-y-4">
  <%= render PanelsUI::PageHeader.new(
        title: "Manage Hotels",
        description: "Review hotel registrations, approvals, and account status across the platform.") do |header| %>
    <% header.with_actions do %>
      <%= render PanelsUI::Button.new(as: :a, href: new_admin_hotel_path) do %>
        <%= app_icon "plus", class: "size-4", aria: { hidden: true } %>
        Add New Hotel
      <% end %>
    <% end %>
  <% end %>

  <%= turbo_frame_tag "hotels_list", aria: { live: "polite" } do %>
    <%= render "registry", presenter: @presenter %>
  <% end %>
</div>

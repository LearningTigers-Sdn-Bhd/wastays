<% is_editing = local_assigns[:editing] %>
<% can_mark_completed = session.status == "scheduled" && session.scheduled_at.present? && session.scheduled_at <= Time.current %>
<<<<<<< HEAD
<% cancellation_reason = session.notes.to_s[/CANCELLED:\s*(.*)\z/m, 1] || session.notes %>
=======
>>>>>>> 07ed162 (fix: prevent onboarding from being marked complete before start date)

<%= turbo_frame_tag dom_id(session) do %>
  <div class="flex flex-col sm:flex-row sm:items-center justify-between p-4 rounded-xl border border-slate-200 gap-4 bg-white">
    <% if is_editing %>
      <%= form_with url: admin_hotel_onboarding_session_path(hotel, session), method: :patch, class: "grid gap-4 sm:grid-cols-2 lg:grid-cols-4 items-end w-full" do |f| %>
        <div class="sm:col-span-1">
          <label class="block text-xs font-semibold text-slate-600 mb-1">Trainer</label>
          <%= f.text_field :trainer_name, value: session.trainer_name, class: "w-full rounded-lg border-slate-200 text-sm" %>
        </div>
        <div class="sm:col-span-1">
          <label class="block text-xs font-semibold text-slate-600 mb-1">Date & Time</label>
          <%= f.datetime_field :scheduled_at, value: session.scheduled_at&.strftime('%Y-%m-%dT%H:%M'), class: "w-full rounded-lg border-slate-200 text-sm" %>
        </div>
        <div class="lg:col-span-1">
          <label class="block text-xs font-semibold text-slate-600 mb-1">Meeting Link</label>
          <%= f.text_field :meeting_link, value: session.meeting_link, class: "w-full rounded-lg border-slate-200 text-sm" %>
        </div>
        <div class="flex gap-2">
          <%= f.submit "Save", class: "flex-1 inline-flex items-center justify-center rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white transition hover:bg-emerald-700" %>
          <%= link_to "Cancel", admin_hotel_onboarding_session_path(hotel, session), class: "flex-1 inline-flex items-center justify-center rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-bold text-slate-700 transition hover:bg-slate-100" %>
        </div>
        <% end %>
        <% else %>
      <div class="flex items-center gap-4">
        <div class="size-10 rounded-full bg-slate-100 flex items-center justify-center text-slate-600">
          <svg class="size-5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        </div>
        <div>
          <p class="text-sm font-bold text-slate-900"><%= session.trainer_name.presence&.titleize || "No trainer assigned" %></p>
          <p class="text-xs text-slate-500">Scheduled: <%= session.scheduled_at.strftime("%d %b %Y at %I:%M %p") %></p>
          <% if session.status == "cancelled" %>
            <div class="mt-2 rounded-xl border border-rose-100 bg-rose-50 px-4 py-3 text-xs text-rose-700">
              <span class="font-semibold">Cancellation note:</span>
              <%= cancellation_reason.presence || "No note provided." %>
            </div>
          <% end %>
        </div>
        </div>

      <div class="flex flex-col gap-3">
      <div class="flex flex-wrap items-center gap-3">
        <% if session.meeting_link.present? %>
          <% if session.status == "scheduled" %>
<<<<<<< HEAD
            <% meeting_url = session.formatted_meeting_link.to_s %>
            <% if meeting_url.start_with?("http://", "https://") %>
              <%= link_to meeting_url, target: "_blank", class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-blue-50 text-blue-700 text-xs font-semibold border border-blue-100 hover:bg-blue-100 transition" do %>
                <svg class="size-3.5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
                Join Call
              <% end %>
=======
            <%= link_to session.formatted_meeting_link, target: "_blank", class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-blue-50 text-blue-700 text-xs font-semibold border border-blue-100 hover:bg-blue-100 transition" do %>
              <svg class="size-3.5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
              Join Call
>>>>>>> 07ed162 (fix: prevent onboarding from being marked complete before start date)
            <% end %>
          <% else %>
            <span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-slate-100 text-slate-400 text-xs font-semibold border border-slate-200 cursor-not-allowed">
              <svg class="size-3.5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
              Join Call
            </span>
          <% end %>
        <% end %>

        <% if session.status == 'scheduled' %>
          <%= link_to edit_admin_hotel_onboarding_session_path(hotel, session), class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-slate-200 bg-white text-slate-600 text-xs font-semibold hover:bg-slate-50 transition" do %>
            <svg class="size-3" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/><path d="m15 5 4 4"/></svg>
            Edit
          <% end %>

<<<<<<< HEAD
          <%= button_to complete_admin_hotel_onboarding_session_path(hotel, session), disabled: !can_mark_completed, class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold border transition #{can_mark_completed ? 'bg-emerald-50 text-emerald-700 border-emerald-100 hover:bg-emerald-100' : 'cursor-not-allowed bg-slate-100 text-slate-400 border-slate-200 opacity-70'}" do %>
=======
          <%= button_to complete_onboarding_session_admin_hotel_path(hotel, session_id: session.id), disabled: !can_mark_completed, class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold border transition #{can_mark_completed ? 'bg-emerald-50 text-emerald-700 border-emerald-100 hover:bg-emerald-100' : 'cursor-not-allowed bg-slate-100 text-slate-400 border-slate-200 opacity-70'}" do %>
>>>>>>> 07ed162 (fix: prevent onboarding from being marked complete before start date)
            <svg class="size-3.5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
            Mark Completed
          <% end %>
          <div class="relative w-fit ml-auto" data-controller="cancel-panel">
            <button type="button"
                    data-cancel-panel-target="trigger"
                    data-action="click->cancel-panel#toggle"
                    aria-expanded="false"
                    class="inline-flex items-center gap-1.5 rounded-xl border border-rose-200 bg-rose-50 px-4 py-2 text-xs font-semibold text-rose-700 transition hover:bg-rose-100">
              Cancel
            </button>
            <div data-cancel-panel-target="panel"
                 class="absolute right-0 z-20 mt-3 hidden w-[320px] rounded-xl border border-slate-200 bg-white p-4 shadow-2xl ring-1 ring-black/10">
              <p class="text-xs text-slate-500">Please provide a reason before cancelling this session.</p>
              <%= form_with url: cancel_admin_hotel_onboarding_session_path(hotel, session), method: :post, class: "mt-3 space-y-3" do |f| %>
                <%= text_area_tag :cancel_reason, nil, rows: 3, required: true, minlength: 10, class: "w-full rounded-sm border-slate-200 text-sm focus:border-rose-400 focus:ring-0" %>
                <div class="flex justify-end">
                  <%= f.submit "Confirm Cancel", class: "inline-flex items-center justify-center rounded-xl bg-rose-600 px-4 py-2 text-xs font-semibold text-white transition hover:bg-rose-700" %>
                </div>
              <% end %>
            </div>
          </div>
        <% else %>
          <% if session.status == "completed" %>
            <span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-emerald-500 text-white text-xs font-semibold border border-emerald-600">
              Completed at <%= session.completed_at&.strftime("%d %b, %H:%M") %>
            </span>
          <% elsif session.status == "cancelled" %>
            <span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-rose-50 text-rose-700 text-xs font-semibold border border-rose-100 transition hover:bg-rose-100">
              Cancelled
            </span>
          <% else %>
            <span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-slate-100 text-slate-500 text-xs font-semibold border border-slate-200">
              Pending
            </span>
          <% end %>
        <% end %>

        <% unless session.status == "completed" %>
          <%= button_to admin_hotel_onboarding_session_path(hotel, session),
                        method: :delete,
                        form: { class: "inline" },
                        data: { turbo_confirm: "Delete this training session permanently?" },
                        class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-rose-200 bg-white text-rose-700 text-xs font-semibold transition hover:bg-rose-50" do %>
            <svg class="size-3.5" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4h8v2"/><path d="M19 6l-1 14H6L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/></svg>
            Delete
          <% end %>
        <% end %>
      </div>
      </div>
    <% end %>
  </div>
<% end %>

<% content_for :title, "Checkout Requested | #{@hotel.name}" %>
<%= render layout: "public/concierge/shared/page_shell",
           locals: { container_class: "w-full max-w-3xl text-center" } do %>
  <%= render "public/concierge/shared/page_header",
             back_path: concierge_home_path(@hotel),
             back_label: "Back to Concierge",
             eyebrow: "Request Sent",
             title_prefix: "Checkout",
             title_accent: "Confirmed!",
             subtitle: "The front desk has been notified. Please proceed to the lobby." %>

  <div class="mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full bg-green-100 text-green-600">
    <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
  </div>

  <%= render "public/concierge/shared/success_booking_card", booking: @booking %>
<% end %>

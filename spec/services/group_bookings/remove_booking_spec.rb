require "rails_helper"

RSpec.describe "GroupBookings::RemoveBooking" do
  it "has focused service coverage" do
    pending("Add focused service coverage for GroupBookings::RemoveBooking")
    raise "pending coverage"
  end
end

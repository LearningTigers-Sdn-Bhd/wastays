require "rails_helper"

RSpec.describe "Bookings::CreateStaffBooking" do
  it "has focused service coverage" do
    pending("Add focused service coverage for Bookings::CreateStaffBooking")
    raise "pending coverage"
  end
end

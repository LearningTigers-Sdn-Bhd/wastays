require "rails_helper"

RSpec.describe "Bookings::SetPrimaryGuest" do
  it "has focused service coverage" do
    pending("Add focused service coverage for Bookings::SetPrimaryGuest")
    raise "pending coverage"
  end
end

require "rails_helper"

RSpec.describe "Bookings::RedesignReadiness" do
  it "has focused service coverage" do
    pending("Add focused service coverage for Bookings::RedesignReadiness")
    raise "pending coverage"
  end
end

require "rails_helper"

RSpec.describe "Billing::SaveGroupArrangement" do
  it "has focused service coverage" do
    pending("Add focused service coverage for Billing::SaveGroupArrangement")
    raise "pending coverage"
  end
end

require "rails_helper"

RSpec.describe "Billing::ApplyGroupArrangement" do
  it "has focused service coverage" do
    pending("Add focused service coverage for Billing::ApplyGroupArrangement")
    raise "pending coverage"
  end
end

require "rails_helper"

RSpec.describe "FolioRouting::RoutingMatrix" do
  it "has focused service coverage" do
    pending("Add focused service coverage for FolioRouting::RoutingMatrix")
    raise "pending coverage"
  end
end

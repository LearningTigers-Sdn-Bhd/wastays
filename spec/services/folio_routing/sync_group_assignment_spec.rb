require "rails_helper"

RSpec.describe "FolioRouting::SyncGroupAssignment" do
  it "has focused service coverage" do
    pending("Add focused service coverage for FolioRouting::SyncGroupAssignment")
    raise "pending coverage"
  end
end
